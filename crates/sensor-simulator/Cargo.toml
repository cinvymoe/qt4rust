[package]
name = "sensor-simulator"
version = "0.2.0"
edition = "2021"
authors = ["Qt Rust Demo Team"]
description = "Unified sensor library supporting both simulated and real sensor interfaces"
license = "MIT"

[dependencies]
thiserror = "1.0"
