[package]
name = "sensor-simulator"
version = "0.2.0"
edition = "2021"
authors = ["Qt Rust Demo Team"]
description = "Simulated sensor library"
license = "MIT"

[dependencies]
crane-data-layer = { path = "../crane-data-layer" }
